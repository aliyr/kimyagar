import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ir.kimiagar.app',
  appName: 'کیمیاگر',
  webDir: 'dist',
  android: {
    allowMixedContent: false,
  },
};

export default config;
